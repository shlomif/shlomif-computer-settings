load_common mymake
load_common completion

base="/home/shlomi/progs/games/nurikabe"
trunk="$base/trunk"
c_src="$trunk/nurikabe-solver"

this="$c_src"

cd "$this"

t()
{
    cd "$this" && make test
}

# Run (test) script:
rs()
{
    cmd="python t/verdict-matrix-tests.py.t"
    cd "$this" && (
    echo "$cmd"
    $cmd
    )
}
