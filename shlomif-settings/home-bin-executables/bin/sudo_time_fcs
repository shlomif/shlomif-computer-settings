#!/bin/bash
echo "USER=$SUDO_USER"
sudo nice -n-20 ionice -c1 -n0 sudo -u "$SUDO_USER" ./freecell-solver-range-parallel-solve "$@"
# sudo nice -n-20 ionice -c1 -n0 sudo -u "$SUDO_USER" ./freecell-solver-multi-thread-solve "$@"
